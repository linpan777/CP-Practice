# 這是 H1 大標題
## 這是 H2 次標題

這是一段普通的文字，包含 **粗體**、*斜體* 和 ~~刪除線~~。

### 1. 程式碼示範
```cpp
// 這是 C++ 代碼
#include <iostream>
int main() {
    std::cout << "Hello World";
    return 0;
}
```
> [!NOTE]
> 這是筆記區域，適合寫補充說明。
>
> [!TIP]
> 這裡可以寫一些小撇步或捷徑。

+ 新增這一行 (綠色)
- 刪除這一行 (紅色)
! 修改這一行 (橘色，不一定支援所有主題)
# 一般註解 (灰色)

- [x] 已完成的工作 (使用 x)
- [ ] 尚未開始的工作 (使用空白)
- [ ] 正在進行中...

- [ ] | 變數名稱 | 意義 | 數值 |
| :--- | :---: | ---: |
| `i` | 層數 | 1 |
| `j` | 空格 | `n-i` |
| `k` | 星星 | `2*i-1` |

請按下 <kbd>Ctrl</kbd> + <kbd>C</kbd> 來複製。
